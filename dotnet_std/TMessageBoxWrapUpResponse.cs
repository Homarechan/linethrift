/**
 * Autogenerated by Thrift Compiler (0.13.0)
 *
 * DO NOT EDIT UNLESS YOU ARE SURE THAT YOU KNOW WHAT YOU ARE DOING
 *  @generated
 */
using System;
using System.Collections;
using System.Collections.Generic;
using System.Text;
using System.IO;
using System.Threading;
using System.Threading.Tasks;
using Thrift;
using Thrift.Collections;

using Thrift.Protocol;
using Thrift.Protocol.Entities;
using Thrift.Protocol.Utilities;
using Thrift.Transport;
using Thrift.Transport.Client;
using Thrift.Transport.Server;
using Thrift.Processor;



public partial class TMessageBoxWrapUpResponse : TBase
{
  private List<TMessageBoxWrapUp> _messageBoxWrapUpList;
  private int _totalSize;

  public List<TMessageBoxWrapUp> MessageBoxWrapUpList
  {
    get
    {
      return _messageBoxWrapUpList;
    }
    set
    {
      __isset.messageBoxWrapUpList = true;
      this._messageBoxWrapUpList = value;
    }
  }

  public int TotalSize
  {
    get
    {
      return _totalSize;
    }
    set
    {
      __isset.totalSize = true;
      this._totalSize = value;
    }
  }


  public Isset __isset;
  public struct Isset
  {
    public bool messageBoxWrapUpList;
    public bool totalSize;
  }

  public TMessageBoxWrapUpResponse()
  {
  }

  public async Task ReadAsync(TProtocol iprot, CancellationToken cancellationToken)
  {
    iprot.IncrementRecursionDepth();
    try
    {
      TField field;
      await iprot.ReadStructBeginAsync(cancellationToken);
      while (true)
      {
        field = await iprot.ReadFieldBeginAsync(cancellationToken);
        if (field.Type == TType.Stop)
        {
          break;
        }

        switch (field.ID)
        {
          case 1:
            if (field.Type == TType.List)
            {
              {
                TList _list263 = await iprot.ReadListBeginAsync(cancellationToken);
                MessageBoxWrapUpList = new List<TMessageBoxWrapUp>(_list263.Count);
                for(int _i264 = 0; _i264 < _list263.Count; ++_i264)
                {
                  TMessageBoxWrapUp _elem265;
                  _elem265 = new TMessageBoxWrapUp();
                  await _elem265.ReadAsync(iprot, cancellationToken);
                  MessageBoxWrapUpList.Add(_elem265);
                }
                await iprot.ReadListEndAsync(cancellationToken);
              }
            }
            else
            {
              await TProtocolUtil.SkipAsync(iprot, field.Type, cancellationToken);
            }
            break;
          case 2:
            if (field.Type == TType.I32)
            {
              TotalSize = await iprot.ReadI32Async(cancellationToken);
            }
            else
            {
              await TProtocolUtil.SkipAsync(iprot, field.Type, cancellationToken);
            }
            break;
          default: 
            await TProtocolUtil.SkipAsync(iprot, field.Type, cancellationToken);
            break;
        }

        await iprot.ReadFieldEndAsync(cancellationToken);
      }

      await iprot.ReadStructEndAsync(cancellationToken);
    }
    finally
    {
      iprot.DecrementRecursionDepth();
    }
  }

  public async Task WriteAsync(TProtocol oprot, CancellationToken cancellationToken)
  {
    oprot.IncrementRecursionDepth();
    try
    {
      var struc = new TStruct("TMessageBoxWrapUpResponse");
      await oprot.WriteStructBeginAsync(struc, cancellationToken);
      var field = new TField();
      if (MessageBoxWrapUpList != null && __isset.messageBoxWrapUpList)
      {
        field.Name = "messageBoxWrapUpList";
        field.Type = TType.List;
        field.ID = 1;
        await oprot.WriteFieldBeginAsync(field, cancellationToken);
        {
          await oprot.WriteListBeginAsync(new TList(TType.Struct, MessageBoxWrapUpList.Count), cancellationToken);
          foreach (TMessageBoxWrapUp _iter266 in MessageBoxWrapUpList)
          {
            await _iter266.WriteAsync(oprot, cancellationToken);
          }
          await oprot.WriteListEndAsync(cancellationToken);
        }
        await oprot.WriteFieldEndAsync(cancellationToken);
      }
      if (__isset.totalSize)
      {
        field.Name = "totalSize";
        field.Type = TType.I32;
        field.ID = 2;
        await oprot.WriteFieldBeginAsync(field, cancellationToken);
        await oprot.WriteI32Async(TotalSize, cancellationToken);
        await oprot.WriteFieldEndAsync(cancellationToken);
      }
      await oprot.WriteFieldStopAsync(cancellationToken);
      await oprot.WriteStructEndAsync(cancellationToken);
    }
    finally
    {
      oprot.DecrementRecursionDepth();
    }
  }

  public override bool Equals(object that)
  {
    var other = that as TMessageBoxWrapUpResponse;
    if (other == null) return false;
    if (ReferenceEquals(this, other)) return true;
    return ((__isset.messageBoxWrapUpList == other.__isset.messageBoxWrapUpList) && ((!__isset.messageBoxWrapUpList) || (TCollections.Equals(MessageBoxWrapUpList, other.MessageBoxWrapUpList))))
      && ((__isset.totalSize == other.__isset.totalSize) && ((!__isset.totalSize) || (System.Object.Equals(TotalSize, other.TotalSize))));
  }

  public override int GetHashCode() {
    int hashcode = 157;
    unchecked {
      if(__isset.messageBoxWrapUpList)
        hashcode = (hashcode * 397) + TCollections.GetHashCode(MessageBoxWrapUpList);
      if(__isset.totalSize)
        hashcode = (hashcode * 397) + TotalSize.GetHashCode();
    }
    return hashcode;
  }

  public override string ToString()
  {
    var sb = new StringBuilder("TMessageBoxWrapUpResponse(");
    bool __first = true;
    if (MessageBoxWrapUpList != null && __isset.messageBoxWrapUpList)
    {
      if(!__first) { sb.Append(", "); }
      __first = false;
      sb.Append("MessageBoxWrapUpList: ");
      sb.Append(MessageBoxWrapUpList);
    }
    if (__isset.totalSize)
    {
      if(!__first) { sb.Append(", "); }
      __first = false;
      sb.Append("TotalSize: ");
      sb.Append(TotalSize);
    }
    sb.Append(")");
    return sb.ToString();
  }
}

