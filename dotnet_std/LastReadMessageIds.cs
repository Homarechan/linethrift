/**
 * Autogenerated by Thrift Compiler (0.13.0)
 *
 * DO NOT EDIT UNLESS YOU ARE SURE THAT YOU KNOW WHAT YOU ARE DOING
 *  @generated
 */
using System;
using System.Collections;
using System.Collections.Generic;
using System.Text;
using System.IO;
using System.Threading;
using System.Threading.Tasks;
using Thrift;
using Thrift.Collections;

using Thrift.Protocol;
using Thrift.Protocol.Entities;
using Thrift.Protocol.Utilities;
using Thrift.Transport;
using Thrift.Transport.Client;
using Thrift.Transport.Server;
using Thrift.Processor;



public partial class LastReadMessageIds : TBase
{
  private string _chatId;
  private List<LastReadMessageId> _lastReadMessageIds;

  public string ChatId
  {
    get
    {
      return _chatId;
    }
    set
    {
      __isset.chatId = true;
      this._chatId = value;
    }
  }

  public List<LastReadMessageId> LastReadMessageIds_
  {
    get
    {
      return _lastReadMessageIds;
    }
    set
    {
      __isset.lastReadMessageIds = true;
      this._lastReadMessageIds = value;
    }
  }


  public Isset __isset;
  public struct Isset
  {
    public bool chatId;
    public bool lastReadMessageIds;
  }

  public LastReadMessageIds()
  {
  }

  public async Task ReadAsync(TProtocol iprot, CancellationToken cancellationToken)
  {
    iprot.IncrementRecursionDepth();
    try
    {
      TField field;
      await iprot.ReadStructBeginAsync(cancellationToken);
      while (true)
      {
        field = await iprot.ReadFieldBeginAsync(cancellationToken);
        if (field.Type == TType.Stop)
        {
          break;
        }

        switch (field.ID)
        {
          case 1:
            if (field.Type == TType.String)
            {
              ChatId = await iprot.ReadStringAsync(cancellationToken);
            }
            else
            {
              await TProtocolUtil.SkipAsync(iprot, field.Type, cancellationToken);
            }
            break;
          case 2:
            if (field.Type == TType.List)
            {
              {
                TList _list149 = await iprot.ReadListBeginAsync(cancellationToken);
                LastReadMessageIds_ = new List<LastReadMessageId>(_list149.Count);
                for(int _i150 = 0; _i150 < _list149.Count; ++_i150)
                {
                  LastReadMessageId _elem151;
                  _elem151 = new LastReadMessageId();
                  await _elem151.ReadAsync(iprot, cancellationToken);
                  LastReadMessageIds_.Add(_elem151);
                }
                await iprot.ReadListEndAsync(cancellationToken);
              }
            }
            else
            {
              await TProtocolUtil.SkipAsync(iprot, field.Type, cancellationToken);
            }
            break;
          default: 
            await TProtocolUtil.SkipAsync(iprot, field.Type, cancellationToken);
            break;
        }

        await iprot.ReadFieldEndAsync(cancellationToken);
      }

      await iprot.ReadStructEndAsync(cancellationToken);
    }
    finally
    {
      iprot.DecrementRecursionDepth();
    }
  }

  public async Task WriteAsync(TProtocol oprot, CancellationToken cancellationToken)
  {
    oprot.IncrementRecursionDepth();
    try
    {
      var struc = new TStruct("LastReadMessageIds");
      await oprot.WriteStructBeginAsync(struc, cancellationToken);
      var field = new TField();
      if (ChatId != null && __isset.chatId)
      {
        field.Name = "chatId";
        field.Type = TType.String;
        field.ID = 1;
        await oprot.WriteFieldBeginAsync(field, cancellationToken);
        await oprot.WriteStringAsync(ChatId, cancellationToken);
        await oprot.WriteFieldEndAsync(cancellationToken);
      }
      if (LastReadMessageIds_ != null && __isset.lastReadMessageIds)
      {
        field.Name = "lastReadMessageIds";
        field.Type = TType.List;
        field.ID = 2;
        await oprot.WriteFieldBeginAsync(field, cancellationToken);
        {
          await oprot.WriteListBeginAsync(new TList(TType.Struct, LastReadMessageIds_.Count), cancellationToken);
          foreach (LastReadMessageId _iter152 in LastReadMessageIds_)
          {
            await _iter152.WriteAsync(oprot, cancellationToken);
          }
          await oprot.WriteListEndAsync(cancellationToken);
        }
        await oprot.WriteFieldEndAsync(cancellationToken);
      }
      await oprot.WriteFieldStopAsync(cancellationToken);
      await oprot.WriteStructEndAsync(cancellationToken);
    }
    finally
    {
      oprot.DecrementRecursionDepth();
    }
  }

  public override bool Equals(object that)
  {
    var other = that as LastReadMessageIds;
    if (other == null) return false;
    if (ReferenceEquals(this, other)) return true;
    return ((__isset.chatId == other.__isset.chatId) && ((!__isset.chatId) || (System.Object.Equals(ChatId, other.ChatId))))
      && ((__isset.lastReadMessageIds == other.__isset.lastReadMessageIds) && ((!__isset.lastReadMessageIds) || (TCollections.Equals(LastReadMessageIds_, other.LastReadMessageIds_))));
  }

  public override int GetHashCode() {
    int hashcode = 157;
    unchecked {
      if(__isset.chatId)
        hashcode = (hashcode * 397) + ChatId.GetHashCode();
      if(__isset.lastReadMessageIds)
        hashcode = (hashcode * 397) + TCollections.GetHashCode(LastReadMessageIds_);
    }
    return hashcode;
  }

  public override string ToString()
  {
    var sb = new StringBuilder("LastReadMessageIds(");
    bool __first = true;
    if (ChatId != null && __isset.chatId)
    {
      if(!__first) { sb.Append(", "); }
      __first = false;
      sb.Append("ChatId: ");
      sb.Append(ChatId);
    }
    if (LastReadMessageIds_ != null && __isset.lastReadMessageIds)
    {
      if(!__first) { sb.Append(", "); }
      __first = false;
      sb.Append("LastReadMessageIds_: ");
      sb.Append(LastReadMessageIds_);
    }
    sb.Append(")");
    return sb.ToString();
  }
}

