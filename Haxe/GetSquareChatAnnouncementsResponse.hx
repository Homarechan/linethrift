/**
 * Autogenerated by Thrift Compiler (0.11.0)
 *
 * DO NOT EDIT UNLESS YOU ARE SURE THAT YOU KNOW WHAT YOU ARE DOING
 *  @generated
 */
package;

import org.apache.thrift.helper.*;
import haxe.io.Bytes;
import haxe.ds.IntMap;
import haxe.ds.StringMap;
import haxe.ds.ObjectMap;

#if flash
import flash.errors.ArgumentError;
#end

import org.apache.thrift.*;
import org.apache.thrift.meta_data.*;
import org.apache.thrift.protocol.*;


class GetSquareChatAnnouncementsResponse implements TBase {
  
  static var STRUCT_DESC = { new TStruct("GetSquareChatAnnouncementsResponse"); };
  static var ANNOUNCEMENTS_FIELD_DESC = { new TField("announcements", TType.LIST, 1); };

  @:isVar
  public var announcements(get,set) : List< SquareChatAnnouncement>;

  inline static var ANNOUNCEMENTS_FIELD_ID : Int = 1;


  public function new() {
  }

  public function get_announcements() : List< SquareChatAnnouncement> {
    return this.announcements;
  }

  public function set_announcements(announcements:List< SquareChatAnnouncement>) : List< SquareChatAnnouncement> {
    this.announcements = announcements;
    return this.announcements;
  }

  public function unsetAnnouncements() : Void {
    this.announcements = null;
  }

  // Returns true if field announcements is set (has been assigned a value) and false otherwise
  public function isSetAnnouncements() : Bool {
    return this.announcements != null;
  }

  public function setFieldValue(fieldID : Int, value : Dynamic) : Void {
    switch (fieldID) {
    case ANNOUNCEMENTS_FIELD_ID:
      if (value == null) {
        unsetAnnouncements();
      } else {
        this.announcements = value;
      }

    default:
      throw new ArgumentError("Field " + fieldID + " doesn't exist!");
    }
  }

  public function getFieldValue(fieldID : Int) : Dynamic {
    switch (fieldID) {
    case ANNOUNCEMENTS_FIELD_ID:
      return this.announcements;
    default:
      throw new ArgumentError("Field " + fieldID + " doesn't exist!");
    }
  }

  // Returns true if field corresponding to fieldID is set (has been assigned a value) and false otherwise
  public function isSet(fieldID : Int) : Bool {
    switch (fieldID) {
    case ANNOUNCEMENTS_FIELD_ID:
      return isSetAnnouncements();
    default:
      throw new ArgumentError("Field " + fieldID + " doesn't exist!");
    }
  }

  public function read( iprot : TProtocol) : Void {
    iprot.IncrementRecursionDepth();
    try
    {
      var field : TField;
      iprot.readStructBegin();
      while (true)
      {
        field = iprot.readFieldBegin();
        if (field.type == TType.STOP) { 
          break;
        }
        switch (field.id)
        {
          case ANNOUNCEMENTS_FIELD_ID:
            if (field.type == TType.LIST) {
              {
                var _list545 = iprot.readListBegin();
                this.announcements = new List< SquareChatAnnouncement>();
                for( _i546 in 0 ... _list545.size)
                {
                  var _elem547 : SquareChatAnnouncement;
                  _elem547 = new SquareChatAnnouncement();
                  _elem547.read(iprot);
                  this.announcements.add(_elem547);
                }
                iprot.readListEnd();
              }
            } else { 
              TProtocolUtil.skip(iprot, field.type);
            }
          default:
            TProtocolUtil.skip(iprot, field.type);
        }
        iprot.readFieldEnd();
      }
      iprot.readStructEnd();

      iprot.DecrementRecursionDepth();
    }
    catch(e:Dynamic)
    {
      iprot.DecrementRecursionDepth();
      throw e;
    }

    // check for required fields of primitive type, which can't be checked in the validate method
    validate();
  }

  public function write(oprot:TProtocol) : Void {
    validate();
    oprot.IncrementRecursionDepth();
    try
    {
      oprot.writeStructBegin(STRUCT_DESC);
      if (this.announcements != null) {
        oprot.writeFieldBegin(ANNOUNCEMENTS_FIELD_DESC);
        {
          oprot.writeListBegin(new TList(TType.STRUCT, this.announcements.length));
          for( elem548 in this.announcements)
          {
            elem548.write(oprot);
          }
          oprot.writeListEnd();
        }
        oprot.writeFieldEnd();
      }
      oprot.writeFieldStop();
      oprot.writeStructEnd();
      oprot.DecrementRecursionDepth();
    }
    catch(e:Dynamic)
    {
      oprot.DecrementRecursionDepth();
      throw e;
    }
  }

  public function toString() : String {
    var ret : String = "GetSquareChatAnnouncementsResponse(";
    var first : Bool = true;

    ret += "announcements:";
    if (this.announcements == null) {
      ret += "null";
    } else {
      ret += this.announcements;
    }
    first = false;
    ret += ")";
    return ret;
  }

  public function validate() : Void {
    // check for required fields
    // check that fields of type enum have valid values
  }

}

