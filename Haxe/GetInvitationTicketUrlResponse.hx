/**
 * Autogenerated by Thrift Compiler (0.13.0)
 *
 * DO NOT EDIT UNLESS YOU ARE SURE THAT YOU KNOW WHAT YOU ARE DOING
 *  @generated
 */
package;

import org.apache.thrift.helper.*;
import haxe.io.Bytes;
import haxe.ds.IntMap;
import haxe.ds.StringMap;
import haxe.ds.ObjectMap;

#if flash
import flash.errors.ArgumentError;
#end

import org.apache.thrift.*;
import org.apache.thrift.meta_data.*;
import org.apache.thrift.protocol.*;


class GetInvitationTicketUrlResponse implements TBase {
  
  static var STRUCT_DESC = { new TStruct("GetInvitationTicketUrlResponse"); };
  static var INVITATION_URL_FIELD_DESC = { new TField("invitationURL", TType.STRING, 1); };

  @:isVar
  public var invitationURL(get,set) : String;

  inline static var INVITATIONURL_FIELD_ID : Int = 1;


  public function new() {
  }

  public function get_invitationURL() : String {
    return this.invitationURL;
  }

  public function set_invitationURL(invitationURL:String) : String {
    this.invitationURL = invitationURL;
    return this.invitationURL;
  }

  public function unsetInvitationURL() : Void {
    this.invitationURL = null;
  }

  // Returns true if field invitationURL is set (has been assigned a value) and false otherwise
  public function isSetInvitationURL() : Bool {
    return this.invitationURL != null;
  }

  public function setFieldValue(fieldID : Int, value : Dynamic) : Void {
    switch (fieldID) {
    case INVITATIONURL_FIELD_ID:
      if (value == null) {
        unsetInvitationURL();
      } else {
        this.invitationURL = value;
      }

    default:
      throw new ArgumentError("Field " + fieldID + " doesn't exist!");
    }
  }

  public function getFieldValue(fieldID : Int) : Dynamic {
    switch (fieldID) {
    case INVITATIONURL_FIELD_ID:
      return this.invitationURL;
    default:
      throw new ArgumentError("Field " + fieldID + " doesn't exist!");
    }
  }

  // Returns true if field corresponding to fieldID is set (has been assigned a value) and false otherwise
  public function isSet(fieldID : Int) : Bool {
    switch (fieldID) {
    case INVITATIONURL_FIELD_ID:
      return isSetInvitationURL();
    default:
      throw new ArgumentError("Field " + fieldID + " doesn't exist!");
    }
  }

  public function read( iprot : TProtocol) : Void {
    iprot.IncrementRecursionDepth();
    try
    {
      var field : TField;
      iprot.readStructBegin();
      while (true)
      {
        field = iprot.readFieldBegin();
        if (field.type == TType.STOP) { 
          break;
        }
        switch (field.id)
        {
          case INVITATIONURL_FIELD_ID:
            if (field.type == TType.STRING) {
              this.invitationURL = iprot.readString();
            } else { 
              TProtocolUtil.skip(iprot, field.type);
            }
          default:
            TProtocolUtil.skip(iprot, field.type);
        }
        iprot.readFieldEnd();
      }
      iprot.readStructEnd();

      iprot.DecrementRecursionDepth();
    }
    catch(e:Dynamic)
    {
      iprot.DecrementRecursionDepth();
      throw e;
    }

    // check for required fields of primitive type, which can't be checked in the validate method
    validate();
  }

  public function write(oprot:TProtocol) : Void {
    validate();
    oprot.IncrementRecursionDepth();
    try
    {
      oprot.writeStructBegin(STRUCT_DESC);
      if (this.invitationURL != null) {
        oprot.writeFieldBegin(INVITATION_URL_FIELD_DESC);
        oprot.writeString(this.invitationURL);
        oprot.writeFieldEnd();
      }
      oprot.writeFieldStop();
      oprot.writeStructEnd();
      oprot.DecrementRecursionDepth();
    }
    catch(e:Dynamic)
    {
      oprot.DecrementRecursionDepth();
      throw e;
    }
  }

  public function toString() : String {
    var ret : String = "GetInvitationTicketUrlResponse(";
    var first : Bool = true;

    ret += "invitationURL:";
    if (this.invitationURL == null) {
      ret += "null";
    } else {
      ret += this.invitationURL;
    }
    first = false;
    ret += ")";
    return ret;
  }

  public function validate() : Void {
    // check for required fields
    // check that fields of type enum have valid values
  }

}

