/**
 * Autogenerated by Thrift Compiler (0.11.0)
 *
 * DO NOT EDIT UNLESS YOU ARE SURE THAT YOU KNOW WHAT YOU ARE DOING
 *  @generated
 */
package;

import org.apache.thrift.helper.*;
import haxe.io.Bytes;
import haxe.ds.IntMap;
import haxe.ds.StringMap;
import haxe.ds.ObjectMap;

#if flash
import flash.errors.ArgumentError;
#end

import org.apache.thrift.*;
import org.apache.thrift.meta_data.*;
import org.apache.thrift.protocol.*;


class ApprovedChannelInfo implements TBase {
  
  static var STRUCT_DESC = { new TStruct("ApprovedChannelInfo"); };
  static var CHANNEL_INFO_FIELD_DESC = { new TField("channelInfo", TType.STRUCT, 1); };
  static var APPROVED_AT_FIELD_DESC = { new TField("approvedAt", TType.I64, 2); };

  @:isVar
  public var channelInfo(get,set) : ChannelInfo;
  @:isVar
  public var approvedAt(get,set) : haxe.Int64;

  inline static var CHANNELINFO_FIELD_ID : Int = 1;
  inline static var APPROVEDAT_FIELD_ID : Int = 2;

  private var __isset_approvedAt : Bool = false;

  public function new() {
  }

  public function get_channelInfo() : ChannelInfo {
    return this.channelInfo;
  }

  public function set_channelInfo(channelInfo:ChannelInfo) : ChannelInfo {
    this.channelInfo = channelInfo;
    return this.channelInfo;
  }

  public function unsetChannelInfo() : Void {
    this.channelInfo = null;
  }

  // Returns true if field channelInfo is set (has been assigned a value) and false otherwise
  public function isSetChannelInfo() : Bool {
    return this.channelInfo != null;
  }

  public function get_approvedAt() : haxe.Int64 {
    return this.approvedAt;
  }

  public function set_approvedAt(approvedAt:haxe.Int64) : haxe.Int64 {
    this.approvedAt = approvedAt;
    this.__isset_approvedAt = true;
    return this.approvedAt;
  }

  public function unsetApprovedAt() : Void {
    this.__isset_approvedAt = false;
  }

  // Returns true if field approvedAt is set (has been assigned a value) and false otherwise
  public function isSetApprovedAt() : Bool {
    return this.__isset_approvedAt;
  }

  public function setFieldValue(fieldID : Int, value : Dynamic) : Void {
    switch (fieldID) {
    case CHANNELINFO_FIELD_ID:
      if (value == null) {
        unsetChannelInfo();
      } else {
        this.channelInfo = value;
      }

    case APPROVEDAT_FIELD_ID:
      if (value == null) {
        unsetApprovedAt();
      } else {
        this.approvedAt = value;
      }

    default:
      throw new ArgumentError("Field " + fieldID + " doesn't exist!");
    }
  }

  public function getFieldValue(fieldID : Int) : Dynamic {
    switch (fieldID) {
    case CHANNELINFO_FIELD_ID:
      return this.channelInfo;
    case APPROVEDAT_FIELD_ID:
      return this.approvedAt;
    default:
      throw new ArgumentError("Field " + fieldID + " doesn't exist!");
    }
  }

  // Returns true if field corresponding to fieldID is set (has been assigned a value) and false otherwise
  public function isSet(fieldID : Int) : Bool {
    switch (fieldID) {
    case CHANNELINFO_FIELD_ID:
      return isSetChannelInfo();
    case APPROVEDAT_FIELD_ID:
      return isSetApprovedAt();
    default:
      throw new ArgumentError("Field " + fieldID + " doesn't exist!");
    }
  }

  public function read( iprot : TProtocol) : Void {
    iprot.IncrementRecursionDepth();
    try
    {
      var field : TField;
      iprot.readStructBegin();
      while (true)
      {
        field = iprot.readFieldBegin();
        if (field.type == TType.STOP) { 
          break;
        }
        switch (field.id)
        {
          case CHANNELINFO_FIELD_ID:
            if (field.type == TType.STRUCT) {
              this.channelInfo = new ChannelInfo();
              this.channelInfo.read(iprot);
            } else { 
              TProtocolUtil.skip(iprot, field.type);
            }
          case APPROVEDAT_FIELD_ID:
            if (field.type == TType.I64) {
              this.approvedAt = iprot.readI64();
              this.__isset_approvedAt = true;
            } else { 
              TProtocolUtil.skip(iprot, field.type);
            }
          default:
            TProtocolUtil.skip(iprot, field.type);
        }
        iprot.readFieldEnd();
      }
      iprot.readStructEnd();

      iprot.DecrementRecursionDepth();
    }
    catch(e:Dynamic)
    {
      iprot.DecrementRecursionDepth();
      throw e;
    }

    // check for required fields of primitive type, which can't be checked in the validate method
    validate();
  }

  public function write(oprot:TProtocol) : Void {
    validate();
    oprot.IncrementRecursionDepth();
    try
    {
      oprot.writeStructBegin(STRUCT_DESC);
      if (this.channelInfo != null) {
        oprot.writeFieldBegin(CHANNEL_INFO_FIELD_DESC);
        this.channelInfo.write(oprot);
        oprot.writeFieldEnd();
      }
      oprot.writeFieldBegin(APPROVED_AT_FIELD_DESC);
      oprot.writeI64(this.approvedAt);
      oprot.writeFieldEnd();
      oprot.writeFieldStop();
      oprot.writeStructEnd();
      oprot.DecrementRecursionDepth();
    }
    catch(e:Dynamic)
    {
      oprot.DecrementRecursionDepth();
      throw e;
    }
  }

  public function toString() : String {
    var ret : String = "ApprovedChannelInfo(";
    var first : Bool = true;

    ret += "channelInfo:";
    if (this.channelInfo == null) {
      ret += "null";
    } else {
      ret += this.channelInfo;
    }
    first = false;
    if (!first) ret +=  ", ";
    ret += "approvedAt:";
    ret += this.approvedAt;
    first = false;
    ret += ")";
    return ret;
  }

  public function validate() : Void {
    // check for required fields
    // check that fields of type enum have valid values
  }

}

